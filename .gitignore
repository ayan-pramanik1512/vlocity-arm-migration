node_modules/
*.js.map
.env

# Build artefacts — regenerated by `npm run build`
lib/

# oclif manifest is committed so `sf plugins install` works without a local build step
# oclif.manifest.json   ← intentionally NOT ignored

# Tool output — generated at runtime, not source
output/
manifest.json
summary-report.txt
